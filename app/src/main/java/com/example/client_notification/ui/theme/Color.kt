package com.example.client_notification.ui.theme
// Definición de colores

import androidx.compose.ui.graphics.Color

// Colores personalizados
val White = Color(0xFFEEF0F2) // Fondo claro
val Background = Color(0xFF07203C) // Azul oscuro (Color principal de la interfaz)
val LightGrayishWhite = Color(0xFFEDEFF1) // Blanco grisáceo (Usado en textos o fondos)
val MediumBlue = Color(0xFF243E57) // Azul medio (Presente en botones o secciones)
val NavyBlue = Color(0xFF061F3B) // Azul marino (Otro tono oscuro predominante)
val DarkGray = Color(0xFF252E3E) // Gris oscuro (Posible para fondos secundarios)
val LightBackground = Color(0xFFF0F2F4) // Fondo claro (Similar al primero)
val DarkBlueVariant = Color(0xFF08213D) // Azul oscuro (Variante del anterior)
val VeryLightWhite = Color(0xFFF1F2F4) // Blanco muy claro (Quizás para textos o elementos resaltados)
val LightGrayishWhiteVariant = Color(0xFFF2F3F4) // Blanco grisáceo (Similar a los anteriores)
val Dark = Color(0xFF242A2D) // Color oscuro adicional
val Yellow = Color(0xFFFFCE00) // Amarillo brillante